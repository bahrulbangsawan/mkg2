/**
 * Cloudflare Worker Environment Bindings
 *
 * This file defines TypeScript types for Cloudflare bindings.
 * Add bindings here as you configure them in wrangler.jsonc.
 *
 * @example
 * interface Env {
 *   // KV Namespace binding
 *   KV_CACHE: KVNamespace;
 *
 *   // D1 Database binding
 *   DB: D1Database;
 *
 *   // R2 Bucket binding
 *   R2_BUCKET: R2Bucket;
 *
 *   // Environment variables
 *   API_KEY: string;
 * }
 */
interface Env {
  // Add your Cloudflare bindings here as needed
}
